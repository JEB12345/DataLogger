Common:

This direcotry contains the pound defines that are used across the project.

It also contains write.c.  This allows printf to properly use a circular buffer for debugging purposes.  Bryant use a different technique but how it works is not fully determined.

